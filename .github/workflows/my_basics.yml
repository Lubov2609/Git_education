name: MyGitHubActionsTest
 
on:
   push:
        branches:
            -master
jobs:   
  my_testing:
   run-on: ubuntu-latest
   steps:
     name: Print Hello Message
     run: echo "Hello world from Testing job"
     
  my_deploy:
   run-on: ubuntu-latest
   steps:
     name: Print Hello Message
     run: echo "Hello world from Deploy job"
